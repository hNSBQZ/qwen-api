# ========== 千问API配置 ==========
# 千问API密钥 - 必须设置
QWEN_API_KEY=your_qwen_api_key_here

# 千问API聊天URL（可选，有默认值）
QWEN_API_CHAT_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions

# 千问聊天模型（可选，有默认值）
QWEN_CHAT_MODEL=qwen3-32b

# 千问音频识别模型（可选，有默认值）
QWEN_AUDIO_RECOGNIZE_MODEL=paraformer-v2

# ========== TTS配置 ==========
# 实时音频URL（可选，有默认值）
REAL_TIME_AUDIO_URL=wss://dashscope.aliyuncs.com/api-ws/v1/realtime?model=qwen-tts-realtime

# TTS语音（可选，有默认值）
TTS_VOICE=Cherry

# TTS采样率（可选，有默认值）
TTS_SAMPLE_RATE=24000

# TTS输出目录（可选，有默认值）
TTS_OUTPUT_DIR=tts_outputs

# ========== 阿里云OSS配置 ==========
# OSS访问密钥ID - 必须设置
ACCESSKEY_ID=your_oss_access_key_id

# OSS访问密钥Secret - 必须设置
ACCESSKEY_SECRET=your_oss_access_key_secret

# OSS存储桶名称（可选，有默认值）
BUCKET=vlm-audio

# OSS区域（可选，有默认值）
REGIN=cn-beijing

# ========== 数据库配置 ==========
# 数据库主机（可选，有默认值）
DB_HOST=192.168.0.164

# 数据库端口（可选，有默认值）
DB_PORT=3306

# 数据库用户名（可选，有默认值）
DB_USER=hqz

# 数据库密码 - 建议设置
DB_PASSWORD=your_database_password

# 数据库名称（可选，有默认值）
DB_NAME=qwen_chat

# ========== 系统配置 ==========
# 默认系统提示词（可选，有默认值）
DEFAULT_SYSTEM_PROMPT=你是一个智能语音助手，具备以下特点：1. 友善、耐心、专业的对话风格 2. 能够理解和回应各种话题 3. 回答简洁明了，避免过于冗长 4. 支持中文和英文交流 5. 在不确定时会诚实地说明 6. 不要在回答中加入无法转换为语音读出的字符

# ========== 音频处理配置 ==========
# FFmpeg路径（必需，用于PCM到MP3转换）
# 默认从系统PATH查找，如果需要指定绝对路径请修改
FFMPEG_PATH=ffmpeg

# ========== FFmpeg安装说明 ==========
# 1. 安装Python依赖：pip install pydub
# 
# 2. 安装FFmpeg（选择适合您系统的方法）：
#
# Windows方法1（简单）：
#   1. 访问 https://github.com/BtbN/FFmpeg-Builds/releases
#   2. 下载 ffmpeg-master-latest-win64-gpl.zip
#   3. 解压到 C:\ffmpeg
#   4. 将 C:\ffmpeg\bin 添加到系统PATH环境变量
#   5. 重启命令行，测试：ffmpeg -version
#
# Windows方法2（指定路径）：
#   如果不想添加到PATH，可以设置：
#   FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe
#
# macOS：
#   brew install ffmpeg
#
# Linux（Ubuntu/Debian）：
#   sudo apt update && sudo apt install ffmpeg
#
# Linux（CentOS/RHEL）：
#   sudo yum install ffmpeg 或 sudo dnf install ffmpeg
#
# 3. 验证安装：在命令行运行 ffmpeg -version